# AWS Configuration
# Note: AWS_REGION and AWS_ACCOUNT are no longer needed here.
# CDK automatically uses CDK_DEFAULT_ACCOUNT and CDK_DEFAULT_REGION
# from your AWS CLI/SDK configuration.
# To override: cdk deploy --profile myprofile

# Email Configuration
TO_EMAIL=your-email@example.com
FROM_EMAIL=verified-sender@example.com

# Secret Names Configuration
# Note: Secret names are now configured in cdk.json context.
# To override, use: cdk deploy --context igSecretName=YOUR_SECRET_NAME
# Default values in cdk.json:
#   - igSecretName: INSTAGRAM_SECRET_KEY
#   - openAiSecretName: OPEN_AI_SECRET_KEY

# Instagram Business Account ID
IG_BUSINESS_ID=your-instagram-business-id

# Stub Mode Flags (set to "true" to use stubbed clients)
# This allows testing end-to-end without hitting real APIs
USE_STUB_IG=true
USE_STUB_EMAIL=true
USE_STUB_LLM=true
USE_STUB_SECRETS=true

# When ready for production, set all stubs to false:
# USE_STUB_IG=false
# USE_STUB_EMAIL=false
# USE_STUB_LLM=false
# USE_STUB_SECRETS=false
